chat:
  build: ../src/
  #command: npm install
  ports:
    - "3000:3000"
  volumes:
    - ../src:/home/node-chat
nginx:
  image: nginx:latest
  ports:
    - "80:80"
  volumes:
    - ../src/chat/public:/usr/share/nginx/html:ro
